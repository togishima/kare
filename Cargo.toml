[workspace]
resolver = "2"
members = ["crates/core", "crates/cli"]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/togishima/kare"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
